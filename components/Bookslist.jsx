import { StatusBar } from 'expo-status-bar';
import { StyleSheet, Text, View } from 'react-native';

export default function Bookslist() {
    return(
        <Text>Hello from Bookslist</Text>
    );
}